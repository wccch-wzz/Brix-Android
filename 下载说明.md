# 📥 下载原版 APK

> 本仓库的 Release 页面提供原版 APK。
> 由于当前发布环境的上传通道限制，**大于 10MB 的文件以「分片」形式存放在仓库根目录**，
> 下载后按下方步骤拼回即可。

## 分片还原（三步）

```bash
# 1. 克隆（或只下载根目录下的 Brix-1.2.apk.part-* 与 manifest.json）
git clone https://github.com/wccch-wzz/Brix-Android.git
cd Brix-Android

# 2. 取还原脚本
curl -O https://raw.githubusercontent.com/wccch-wzz/Brix-Android-Source/main/tools/join_parts.py

# 3. 拼接并自动校验 sha256
python3 join_parts.py --manifest Brix-1.2.apk.manifest.json
```

看到 `✅ 校验通过，文件已还原` 即成功。

也可以不用脚本，直接拼接：

```bash
cat Brix-1.2.apk.part-* > Brix-1.2.apk
sha256sum Brix-1.2.apk   # 与 manifest.json 里的 sha256 对照
```

## 当前版本

| 项 | 值 |
|---|---|
| 文件 | `Brix-1.2.apk` |
| 大小 | 47,037,627 bytes |
| sha256 | `bae87937b49c48919c05191848243d41fd50bd140f626b359dd5314ac0980622` |
| md5 | `7c3136978e3ebfe03463424181f8d326` |
| 分片数 | 6 |
| 签名 | v2 有效 |
| 包名 | `com.brix.brixlauncher` |

## 🔧 想要可编译的源码？

原版 APK 的反编译源码已整理成**可直接编译**的独立工程：

➡️ **[wccch-wzz/Brix-Android-Source](https://github.com/wccch-wzz/Brix-Android-Source)**

- 840 个业务源文件 + 57 个依赖 jar
- 克隆后 `./build.sh` 即可编译（实测 **0 error**，产出 2283 个 class）
- 每次检测到新版会自动重新构建并发布源码 Release
